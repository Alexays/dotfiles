#!/bin/bash

sudo pacman --noconfirm -Rscn $(pacman -Qdtq)
sudo pacman --noconfirm -Sc
sudo pacman-optimize
sudo sync

if [ -x "/usr/bi/updatedb" ]; then
   sudo updatedb
fi

echo $0: Finished processing!
exit 0